# Ghost App Visuals

Native macOS app screenshots should be captured here when the Ghost menu-bar panel is available in an interactive desktop session with the required permissions granted.

Current committed files are labeled visual previews, not native screenshots:

```text
main-panel-preview.svg
provider-routing-preview.svg
rag-memory-preview.svg
agent-mode-preview.svg
verified-actions-preview.svg
ghost-code-preview.svg
```

Expected files:

```text
main-panel.png
model-routing.png
rag-query.png
agent-mode.png
settings.png
```
